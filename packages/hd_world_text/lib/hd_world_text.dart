/// hd_world 의 한국어 이름표.
///
/// 모델은 키만 들고 있고 이 패키지가 말로 바꾼다. 콘솔·웹·Flutter 가
/// **같은 표**를 봐야 화면마다 이름이 갈리지 않는다 —
/// `hd_battle_text` 와 같은 자리다.
library;

export 'src/item_names.dart';
export 'src/names.dart';
