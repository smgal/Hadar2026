name: cm2_script
description: Minimal script parser and interpreter with pluggable commands and functions.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dev_dependencies:
  test: ^1.24.0
